## خطة التحسينات الهيكلية (3 تحسينات، بلا تغيير سلوك)

### 1) تقسيم server/src/routes/admin.ts (924 سطراً)
إنشاء مجلد server/src/routes/admin/ بمُوجِّهات فرعية + ملف تجميع رفيع:
- shared.ts: DbAuditLogRow + mapAuditRow (مشتركة بين audit-logs وجلسات البائع)
- settings.ts: GET/PUT /settings + DbSystemSettingsRow/mapAdminSettingsToCamelCase
- transactions.ts: GET /transactions
- identities.ts: GET /duplicate-identities + POST flag/block/unblock + logIdentityAction + أنواع DbDuplicate*
- audit.ts: GET /audit-logs (يستورد mapAuditRow من shared)
- sellers.ts: GET /sellers + PUT /sellers/:id/status + GET /sellers/:id/sessions + ADMIN_SELLERS_SELECT/mapAdminSeller/toCoord
- batch.ts: POST /sims/batch + MAX_BATCH_SIMS
- system.ts: POST /reset + POST/GET system/backup + POST/GET system/lockdown(/status) + GET /monitoring
- bootstrap.ts: ensureIdentityRiskSchema + ensureSessionSchema (تشغيل عند التحميل إن NODE_ENV!=='test')
- index.ts: router.use(...) لكل فرعي + تشغيل bootstrap + إعادة تصدير router الافتراضي و ensureIdentityRiskSchema
كل المسارات/الأدوار/الاستجابات تبقى كما هي؛ كل فرعي يستورد اعتمادياته.

### 2) دمج شاشات الشرائح المتوازية (القائمة على نوع Sim)
- sims/operators.ts: تهيئة OPERATORS الموحّدة + operatorName(key) لإزالة المصفوفات المكررة وتراكيب يمن_موبايل?...
- sims/SimsListView.tsx: مكوّن واحد (sims: Sim[], mode:'agent'|'seller', onUpdateSims?) يعرض الهيكل المشترك (بطاقات المشغّل + بحث + تبويبات حالة + جدول/بطاقات + ترقيم) ويعرض قائمة الإجراءات والنوافذ فقط إن وُجد onUpdateSims.
- حذف SellerSimsView.tsx و agent/SimManagementView.tsx؛ تحديث SellerDashboard.tsx و AgentDashboard.tsx لاستخدام SimsListView.
- يُبقى SIMsView.tsx (المدير) منفصلاً: يستخدم نوع SIM مختلفاً (provider/packageType/owner) ومكوّنات sims/ الحديثة + CSV/دفعات؛ دمجه سيتطلب توحيد الأنواع وخطر انحراف سلوكي.

### 3) توحيد مكوّنات التنقل
- nav/navItems.ts: مصدر وحيد لـ managerNavItems/agentNavItems/sellerNavItems (id/label/icon) و viewTitles: Record<ViewType,string>؛ تستهلكه TopBar/BottomNav/NavBar.
- nav/ManagerChrome.tsx: يجمع TopBar + BottomNav لمكوّن واحد؛ App.tsx يعرضه بدل المكوّنين المنفصلين ويزيل ربط setView المكرر.
- NavBar (وكيل/بائع) يستورد قوائمه من navItems.ts (سلوكه ثابت). MobileBottomNav و AdminMoreDrawer يبقيان مشتركين.

### بوابات التحقق
1) npm run lint  2) cd server && npx tsc --noEmit  3) npm run build  4) npx vitest run
(تركيز على inventory-flow + اختبارات الأمان). لا تغيير Android.

### ملفات متأثرة
إضافة ~11 ملفاً (routes/admin/*، sims/operators.ts، sims/SimsListView.tsx، nav/navItems.ts، nav/ManagerChrome.tsx)؛
تعديل admin.ts(index)، App.tsx، SellerDashboard.tsx، AgentDashboard.tsx، TopBar.tsx، BottomNav.tsx، NavBar.tsx؛
حذف SellerSimsView.tsx، agent/SimManagementView.tsx.