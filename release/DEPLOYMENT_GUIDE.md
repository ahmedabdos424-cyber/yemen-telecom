# Deployment Guide ΓÇö Yemen Telecom## 1. Backend (Render, already live)- **Service:** `yemen-telecom` (web_service, Docker) ΓÇö `https://yemen-telecom.onrender.com`- **Health:** `GET /api/health` ΓåÆ `{ "status": "ok", "db": "connected" }` (HTTP 200)- **Deploy model:** auto-deploy from `main` on push.- **Last deploy:** `dep-d9b8t79oagis739migag` (commit `212eefe`, status `live`).### Emergency / manual deploy```powershellgit push origin main   # triggers Render auto-deploy```Monitor: `https://dashboard.render.com/web/srv-d91hulpkh4rs73ac2fg0`### Environment variables required (verify existence, never expose values)`NODE_ENV, API_PORT, DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME, DB_SSL_REJECT_UNAUTHORIZED,DB_SSL_CA_CERT, DB_FAMILY, DB_MAX_CONNECTIONS, DB_SLOW_QUERY_MS, JWT_SECRET, REFRESH_SECRET,CSRF_SECRET, CORS_ORIGIN, SUPABASE_URL, SUPABASE_ANON_KEY, UPLOAD_BUCKET,BACKUP_S3_ENDPOINT, BACKUP_S3_REGION, BACKUP_S3_ACCESS_KEY_ID, BACKUP_S3_SECRET_ACCESS_KEY, BACKUP_S3_BUCKET`## 2. Web SPA (served by the same Render service)The SPA is built into `dist/` and served by the backend. After a backend deploy the SPA is liveautomatically. Verified at `https://yemen-telecom.onrender.com` ΓÇö login + manager dashboard renderin Arabic RTL.> **Cold-start note:** On the free tier the origin spins down after inactivity; the first visit after> idle may briefly fail to load bundles (HTTP 500) then recover once warm. For guaranteed 24/7 uptime> upgrade the Render plan to Starter+ or host the static SPA on Netlify/Firebase Hosting (already> referenced in CORS allowlist).## 3. Android (Google Play)- Build signed APK/AAB (see `SIGNING_GUIDE.md`).- Upload `release/yemen-telecom-release.aab` to Play Console ΓåÆ Production track.- Complete store listing, screenshots, feature graphic, Data Safety, Content Rating (see  `GOOGLE_PLAY_CHECKLIST.md`).- Roll out gradually (staged 10% ΓåÆ 100%).## 4. Rollback- Backend: redeploy previous healthy deploy in Render dashboard.- Android: upload a prior `versionCode` is NOT allowed; instead roll forward with a new build or use  Play Console "halt rollout".